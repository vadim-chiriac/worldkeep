kernel_version: "0.19"
name: "The Lower Fen"
calendar: ""            # set once the author names one; vagueness is legal
facets: [when, where, valence, weight, members, status, amount, value, fiat]
std_types: [part_of, subordinate_to, holds, opposes, participates, action, action/practice, period, state, precedes]
extensions:
  - scribe              # scribe.origin, scribe.session (SCRIBE.md §6)
# present: entities/<period>   # the world's "now"; set once a present exists
